= Urban Function Example (i-UR)

== Purpose

This example demonstrates working with complex, real-world schemas that have extensive import/include dependencies.

Learn how to:

* Build LXR packages from schemas with many imports
* Configure schema location mappings for offline work
* Handle large schema sets (100+ types)
* Work with geographic/urban planning standards

The i-UR (international Urban Revitalization) standard is used here to showcase real-world complexity found in Japanese urban planning data exchange formats.

== What This Example Demonstrates

=== Complex Schema Dependencies

The i-UR schema imports multiple other schema families:

* GML (Geography Markup Language) 3.2
* ISO 19139 (Geographic Metadata)
* XLink (XML Linking)
* CityGML extensions

Each import requires proper schema location mapping for offline processing.

=== Schema Location Mapping

Mapping relative schema locations to local files:

[source,yaml]
----
schema_location_mappings:
  - from: "../../uro/3.2/urbanObject.xsd"
    to: ../../spec/fixtures/i-ur/urbanObject.xsd
  - from: '(?:\.\./)+(gml/.+\.xsd)$'
    to: ../../spec/fixtures/codesynthesis-gml-3.2.1/\1
    pattern: true
----

This allows building packages without network access.

=== Large Schema Sets

The urban function schema defines 200+ types including:

* Zone types (residential, commercial, industrial)
* District types (planning, preservation)
* Urban facility types (parks, roads, utilities)
* Development project types

=== Domain-Specific Vocabulary

Urban planning concepts modeled:

* Land use classifications
* Zoning regulations
* Urban development projects
* Infrastructure planning

== Directory Structure

[source]
----
02-urban-function/
├── README.adoc           # This file
├── config.yml            # Package configuration with mappings
├── schemas/              # (empty - schemas in spec/fixtures)
├── outputs/              # Generated output files
│   └── urban_function.lxr    # Built LXR package
│   └── urban_function_docs.html  # HTML documentation (SPA)
├── ruby-api/             # Ruby API examples
│   └── example.rb        # Builds package AND generates HTML docs
└── cli/                  # CLI examples
    └── run.sh            # Builds package AND generates HTML docs
----

== Configuration

The [`config.yml`](config.yml) file is validated against a JSON Schema at
link:../../docs/schemas/lxr-package-config.schema.json[]. It supports the following sections:

[source,yaml]
----
# === Package Metadata ===
metadata:
  name: "i-UR Urban Function Schemas"
  version: "3.2.0"
  title: "i-UR Urban Function"           # Display name (used in SPA header)
  description: "Japanese i-UR standard schemas for urban planning."
  license: "MIT"                          # SPDX license identifier
  authors:
    - name: "Geospatial Japan"
      email: "info@geospatial.jp"
  homepage: "https://www.geospatial.jp/iur/"
  repository: "https://github.com/lutaml/lutaml-xsd"
  documentation: "https://lutaml.github.io/lutaml-xsd/"
  tags: [urban-planning, gml, japan, i-ur, geospatial]

# === Build Options ===
build:
  xsd_mode: include_all           # include_all | allow_external
  resolution_mode: resolved       # bare | resolved
  serialization_format: marshal    # marshal | json | yaml | parse

# === Schema Entry Points ===
files:
  - ../../spec/fixtures/i-ur/urbanFunction.xsd

# === Schema Location Mappings ===
schema_location_mappings:
  - from: "../../uro/3.2/urbanObject.xsd"
    to: ../../spec/fixtures/i-ur/urbanObject.xsd
    pattern: false

# === Namespace Mappings ===
namespace_mappings:
  - prefix: "urf"
    uri: "https://www.geospatial.jp/iur/urf/3.2"
  - prefix: "gml"
    uri: "http://www.opengis.net/gml/3.2"

# === Appearance (for SPA) ===
appearance:
  logos:
    long:
      light:
        path: "images/logo-plateau_logo-long.svg"
      dark:
        path: "images/logo-plateau_logo-long-dark.svg"
  colors:
    primary: "#5b9cd4"
    accent: "#dbab3e"

# === External Documentation Links ===
links:
  getting_started: "https://lutaml.github.io/lutaml-xsd/getting-started"
  api_reference: "https://lutaml.github.io/lutaml-xsd/api"
  examples: "https://lutaml.github.io/lutaml-xsd/examples"
  issue_tracker: "https://github.com/lutaml/lutaml-xsd/issues"
----

Key sections:

`metadata.name` and `metadata.version` (required):: Package identity
`metadata.title`:: Display name used in the SPA header
`metadata.authors`:: Array of author objects (name, email)
`metadata.license`:: SPDX license identifier (e.g., MIT, Apache-2.0)
`metadata.tags`:: Array of strings for discoverability
`build`:: Controls XSD bundling, resolution, and serialization format
`appearance`:: Logo paths, colors, typography, and custom CSS for the SPA
`links`:: External documentation links shown in the SPA

The `files`, `schema_location_mappings`, and `namespace_mappings` sections
are the same as in previous versions.

== Running the Examples

=== Ruby API Example

[source,bash]
----
ruby examples/02-urban-function/ruby-api/example.rb
----

Features demonstrated:

. Building packages with schema location mappings
. Handling large type counts efficiently
. Batch type queries
. Namespace-aware searching
. Generating interactive HTML documentation (SPA) from the package

=== CLI Example

[source,bash]
----
bash examples/02-urban-function/cli/run.sh
----

CLI commands demonstrated:

. Building with complex configurations
. Package tree visualization
. Searching in large schema sets
. Type resolution across namespaces
. Generating interactive HTML documentation (SPA) from the package

== Generating Interactive HTML Documentation (SPA)

The SPA is a self-contained HTML file with embedded CSS and JavaScript that provides an interactive documentation browser for your schema package.

=== CLI Generation

[source,bash]
----
# Build the LXR package first
bundle exec exe/lutaml-xsd build from-config config.yml --output urban_function.lxr

# Generate the SPA documentation
bundle exec exe/lutaml-xsd doc spa urban_function.lxr --mode vue_inlined --output urban_function_docs.html
----

Options:

* `--mode vue_inlined` - Embeds all assets in a single HTML file (default, recommended)
* `--mode vue_cdn` - Loads Vue from CDN (requires internet)

=== Ruby API Generation

[source,ruby]
----
require 'lutaml/xsd'

# Build the package
repository = Lutaml::Xsd::SchemaRepository.from_file('config.yml')
repository.parse.resolve

# Generate the SPA
generator = Lutaml::Xsd::Spa::Generator.new(
  repository.to_package,
  'urban_function_docs.html',
  mode: 'vue_inlined'
)
generator.generate
----

=== Configuration

Customize the SPA appearance via the `appearance` section in `config.yml`:

[source,yaml]
----
appearance:
  logos:
    long:
      light:
        path: "images/logo-plateau_logo-long.svg"
      dark:
        path: "images/logo-plateau_logo-long-dark.svg"
  colors:
    primary: "#5b9cd4"
    accent: "#dbab3e"
  typography:
    font_family: "system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif"
  custom_css: |
    /* Custom CSS injected into the SPA */
----

=== Output

Creates `urban_function_docs.html` (interactive SPA):

* Browse schemas, types, elements, groups, attribute groups
* Search across all namespaces
* View type definitions with XML-like syntax
* Navigate type references by clicking
* Dark/light mode toggle
* Responsive sidebar with collapsible tree

== Expected Output

Creates `urban_function.lxr` containing:

* Schemas: 50+ (from all imports)
* Types: 200+ (urban planning types)
* Namespaces: 10+ (urf, uro, gml, xlink, etc.)

Building may take 5-10 seconds due to schema complexity.

== Key Concepts

=== Offline Schema Processing

Schema location mappings enable:

* Working without internet connectivity
* Consistent build results
* Version control of schema dependencies
* Faster builds (no network delays)

=== Pattern-Based Mappings

Regular expressions in mappings support:

* Multiple files with one rule
* Flexible path transformations
* Import path normalization
* Dependency consolidation

=== Real-World Schema Complexity

This example represents realistic scenarios:

* Government data standards
* Industry specifications
* Geographic information systems
* Multi-organization schemas

== Troubleshooting

=== Import Resolution Failures

If imports fail to resolve:

. Check that fixture files exist in `spec/fixtures/`
. Verify schema location mapping patterns
. Enable verbose output for debugging

=== Build Performance

For large schemas:

* Use Marshal serialization (fastest)
* Enable caching for development
* Consider resolution mode options

== Next Steps

After mastering complex schemas:

. Try link:../03-unitsml/README.adoc[03-unitsml] for scientific domain schemas
. Try link:../04-metaschema/README.adoc[04-metaschema] for self-referential schemas
. Review link:../01-simple/README.adoc[01-simple] for basic concepts

== Related Documentation

* link:../../docs/PACKAGE_CONFIGURATION.adoc[Package Configuration]
* link:../../docs/SCHEMA_LOCATION_MAPPING.adoc[Schema Location Mapping]
* link:../../examples/urban_function_repository.yml[Full i-UR Configuration]

== i-UR Standard Reference

* Official URL: https://www.geospatial.jp/iur/
* Standard Version: 3.2
* Domain: Urban planning and revitalization (Japan)